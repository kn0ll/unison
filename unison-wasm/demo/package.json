{
  "name": "unison-wasm-demo",
  "version": "0.1.0",
  "description": "Unison WASM Demo - Price Calculator showing one-program fullstack",
  "type": "module",
  "scripts": {
    "build:ts": "tsc",
    "build": "bash scripts/setup.sh && bash scripts/build-codebase.sh && bash scripts/compile-codebase.sh && bash scripts/generate-types.sh && npm run build:ts && cp index.html dist && cp -r .unison dist",
    "clean": "rm -rf dist .unison"
  },
  "dependencies": {
    "@unison/wasm-runtime": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
